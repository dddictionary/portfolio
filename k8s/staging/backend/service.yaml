apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: portfolio-staging
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: backend
