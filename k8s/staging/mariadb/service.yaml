apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: portfolio-staging
  labels:
    app: mariadb
spec:
  clusterIP: None
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: mariadb
